# codealpha_task2
A Python chatbot is a small program that takes user input and responds with predefined answers. It uses simple logic (like **if-else statements**) to reply to messages typed by the user.
